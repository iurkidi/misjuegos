{% extends 'uniMisJuegosBundle::layout.html.twig' %}

{% block central %} 
    <h1>tipo list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
               {# <th>Actions</th>#}
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.id }}</td>
                <td><a href="{{ path('tipo_show', { 'id': entity.id }) }}">{{ entity.nombre }}</a></td>
               {# <td>
                <ul>
                    <li>
                        <a href="{{ path('tipo_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tipo_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>#}
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tipo_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
